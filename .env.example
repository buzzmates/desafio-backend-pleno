DB_TYPE=postgres
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=change-me
DB_NAME=orders_orchestrator

REDIS_HOST=localhost
REDIS_PORT=6379
# If you run the Nest app locally and only Redis via docker compose,
# change REDIS_PORT to 6380.

EXCHANGE_RATE_API_KEY=your_exchange_rate_api_key